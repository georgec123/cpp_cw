#pragma once

#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>
#include <stdlib.h>

void print_fraction(int numerator, int denominator);
int check_fraction(int numerator, int denominator);
int frac_sleepy(int numerator, int denominator);